
-----

# 🎵 Music Player — JavaFX + Spring Boot + MySQL

> **Застосунок для відтворення музики з підтримкою плейлістів.**

## Посилання на вже готові скомпільовані jar-файли (client & server):

[Посилання](https://dropmefiles.com.ua/ua/SZDV4YUB)

## 📌 Опис проєкту

Проєкт складається з двох окремих частин:

### ▶️ 1. Клієнт (JavaFX)

Графічний застосунок, який дозволяє:

  * Імпортувати локальні аудіофайли.
  * Відтворювати треки.
  * Перемикати **Next / Prev**.
  * Вмикати **Shuffle / Repeat**.
  * Створювати, видаляти і редагувати плейлісти.
  * Зберігати плейлісти на сервер.

### 🖥️ 2. Сервер (Spring Boot)

Забезпечує:

  * REST API для клієнта.
  * Збереження плейлістів та треків у MySQL.
  * Логіку додавання/видалення треків у плейлисті.

-----

## 📂 Структура репозиторію

```text
/TRPZ_coursework/
 ├─ client/        # гілка client — код JavaFX
 ├─ server/        # гілка server — код Spring Boot
 └─ main/          # головна гілка (README.md)
```

-----

## 🚀 Як запустити систему з нуля

### 1️⃣ Встановлення залежностей

Потрібно встановити наступні компоненти:

| Компонент | Версія |
| :--- | :--- |
| **JDK** | ≥ 21 |
| **Maven** | ≥ 3.9 |
| **MySQL Server** | ≥ 8.0 |

### 2️⃣ Налаштування бази даних MySQL

Після встановлення MySQL, відкрийте консоль і створіть базу:

```sql
CREATE DATABASE music_player_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

> **Примітка:** За замовчуванням сервер використовує `username = root` та `password = (ваш пароль root при встановленні MySQL)`.

> ОБОВ'ЯЗКОВО ЗМІНИТИ username та password *під ваші у application.properties серверної частини (src/main/resources)*

### 3️⃣ Запуск сервера (Spring Boot)

Перейдіть у папку серверної частини:

```bash
cd music-player-server
```

**✔ Зібрати сервер:**

```bash
mvn clean package
```

У папці `target/` з’явиться файл `music-player-server-1.0.0.jar`.

**✔ Запустити сервер:**

```cmd
run-server.bat
```

Після запуску сервер буде доступний за адресою: `http://localhost:8080/api`

### 4️⃣ Запуск клієнта (JavaFX)

#### Варіант А — запуск із вихідного коду (через IntelliJ IDEA)

1.  Відкрити проєкт `music-player-client` в IntelliJ.
2.  Встановити **JDK 21** як SDK проєкту.
3.  Запустити клас: `com.example.musicclient.MainApp`.

*Клієнт відкриється у вигляді JavaFX-вікна.*

#### Варіант B — запуск у вигляді EXE (згенерованого jpackage)

Після збірки через jpackage буде папка:

```text
MusicPlayer/
 ├─ MusicPlayer.exe
 ├─ runtime/
 ├─ app/
 └─ інші службові файли
```

**Запуск:**
Запустіть файл `MusicPlayer.exe`.

-----

## 🧩 Технології проєкту

### 🎨 Клієнт

  * **JavaFX 21**
  * MediaPlayer API
  * HTTP REST Client (HttpURLConnection / Java 11 HttpClient)
  * **Патерни:**
      * Facade (`AudioPlayerFacade`)
      * Iterator (`Playlist traversal`)

### ☕ Сервер

  * **Spring Boot**
  * Spring Web (REST API)
  * Spring Data JPA / Hibernate
  * MySQL 8
  * Maven

-----

## 🖥️ Скріншоти
<img width="900" height="626" alt="image" src="https://github.com/user-attachments/assets/5d6ad534-2265-4326-869c-d81ee3554636" />

<img width="902" height="627" alt="image" src="https://github.com/user-attachments/assets/33253f77-80b5-4362-a4c9-9b82ea15b1ad" />


